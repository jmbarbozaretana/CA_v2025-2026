\documentclass[class=article,border=1pt]{standalone}
\usepackage[europeanresistors,americaninductors,RPvoltages]{circuitikz}
\usetikzlibrary{calc}
\usepackage{verbatim}
\usepackage{textcomp}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows}
\usepackage{amsmath}


\begin{document}
\begin{circuitikz}[american, scale=0.85]
  %% Source
  \coordinate (Csource);
  \foreach \anch/\ang in {a/0,b/120,c/240}{%
    \draw (Csource) to[sV=$\mathbf{V}_{\anch n} $,*-,american] +({-\ang+90}:3) coordinate (S\anch) ;
  }
  % Load
  \coordinate (Cload)  at ($(Csource)+(10,0)$);
  \foreach \anch/\ang in {a/0,b/-120,c/-240}{%
    \draw (Cload)+({-\ang+90}:3) to[generic,l=$\mathbf{Z}_{\Delta}$,-] +({-\ang+90+120}:3) coordinate (L\anch);
  }
  
  \draw (Csource) node[above left]{$n$};
  %\draw (Cload) node[above right]{N};
  
  %% Connections
  \draw ($(Sa)+(2.5,0)$) to [generic,l=$\mathbf{Z}_L$,i=$\mathbf{I}_{a}$] ($(Lc)+(-2.5,0)$);
  \draw (Sa) -- ($(Sa)+(2.5,0)$);
  \draw (Lc) -- ($(Lc)+(-2.5,0)$);
  \draw (Sb) to[generic,l=$\mathbf{Z}_L$,i=$\mathbf{I}_{b}$] (La);
  
  
  \draw ($(Sc)+(5,-2)$) to [generic,l=$\mathbf{Z}_L$,i=$\mathbf{I}_{c}$] ($(Lb)+(-5,-2)$);
  \draw (Sc) -- +(0,-2);
  \draw ($(Lb)+(-5,-2)$) to [short,-]  ($(Lb)+(0,-2)$) -- (Lb);
  \draw ($(Sc)+(0,-2)$) to [short,-]  ($(Sc)+(5,-2)$);
  
  %\draw (Csource) -- (Cload);
\end{circuitikz}
\end{document}
